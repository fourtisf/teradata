node_modules/
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo

.env
.env.local
.env*.local

# The post ledger, when it is left at its development default. On the VPS it
# lives in /var/lib/tare/social — a record of what has been published should
# not sit inside a checkout that gets pulled over.
.tare/

.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

.vercel
